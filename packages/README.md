# packages/

Shared cross-cutting packages reusable by `apps/*`.

## Layout
- `shared-types/` — language-agnostic type contracts (e.g. JSON schemas, generated TS types from OpenAPI).
- `shared-utils/` — small reusable utilities (formatters, constants, validation helpers).

## Status
Skeleton placeholder. Populated as needs emerge across services.
