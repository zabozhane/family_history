# packages/shared-types

Cross-language type contracts shared between `apps/api` and `apps/web`.

Planned content:
- OpenAPI-generated TypeScript types.
- Common enum mirrors (asset types, permission scopes, user roles).
