[tool.pyright]
include = ["app"]
exclude = ["**/migrations"]
pythonVersion = "3.12"
typeCheckingMode = "standard"
# Resolve third-party imports after: pip install -r requirements.txt -t .venv ...
venvPath = "."
venv = ".venv"
