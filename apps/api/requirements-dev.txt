# Optional dev tooling (Pyright). Install into the same venv as requirements.txt:
#   cd apps/api && python -m venv .venv && .venv/bin/pip install -r requirements.txt -r requirements-dev.txt
pyright>=1.1.389
