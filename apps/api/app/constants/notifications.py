"""Notification ``kind`` string constants (single source of truth)."""

JOIN_REQUEST_PENDING = "join_request_pending"
JOIN_REQUEST_APPROVED = "join_request_approved"
JOIN_REQUEST_REJECTED = "join_request_rejected"
